# `codex/alpha-0.3.6` 功能与档案审核报告

日期：2026-07-15

## 审核范围

- 固定比较点：本地 `main` 与 `origin/main`，共同 merge-base `dfc1301`。
- 审核前分支 HEAD：`fb66f3a`；相对 main 共 20 个提交、53 个文件，约新增 2995 行、删除 440 行。
- 审核轴：项目规范（Standards）与用户需求（Spec）。当前会话受约束未启用子代理，两轴由同一会话分别检查。
- 事实分层：自动化结果为本会话亲自运行；模拟器交互为用户报告；Worker 部署与 D1 测试写入以本机权威台账为准。

## 结论

**可提交 main 审核，无代码阻断项。** 分层、l10n、异步账号守卫、Worker 鉴权/参数化查询、隐私边界和受保护文件均符合项目规则。九组需求均有实现和测试/人工证据。

发现的问题属于档案漂移而非功能缺陷：旧首页截图被称为当前截图、自定义头像设计仍标记尚未实现、`0.3.6` 记录仍写举报/屏蔽未执行、旧待办仍要求重复核对已完成的 Worker 能力、info 快照和最新交接入口落后。以上在本轮文档提交中修正。

## 需求覆盖

| 需求 | 主要提交 | 验证与边界 |
|---|---|---|
| 记录页周/月/年及历史周期横向切换动效 | `c7d0a15` | Widget 方向/减弱动效测试；用户报告滑动与按钮切换通过 |
| App 内唯一账号资料源、首页缓存头像快速展示 | `c7d0a15`、`05a5652` | `AccountController.user` + 安全存储缓存；缓存不授予会员权限，401 仍清理 session |
| 加入公开范围说明、退出二次确认 | `c7d0a15` | 排行榜/个人页复用确认逻辑；Widget 与用户交互验收通过 |
| 编辑资料失败提示前移并保留输入 | `c7d0a15` | 标题下持久错误横幅，测试确认位于输入框上方且不泄露异常原文 |
| 首页训练卡、运动广场卡、无性别剪影及浅深色统一 | `56ad321`、`007bf05`、`4c6d07e`、`0185b34`、`10532fc` | 窄屏/主题 Widget；用户多轮浅深色截图验收通过 |
| 隐藏首页测试入口 | `c7d0a15` | Debug/Release 首页均不展示；测试能力代码保留 |
| 原生 Splash、启动门、三页引导和相机用途说明 | `05a5652` | 首次/回访、Next、跳过、权限说明测试；用户报告三页和按钮通过 |
| 持久屏蔽名单与解除屏蔽 | `2692367`、`c723cdb` | Flutter/Worker 自动化；Worker 已部署；用户完成屏蔽→隐藏→名单→解除→恢复 |
| 长按举报/屏蔽面板与举报加载反馈 | `bf198b6`、`fb66f3a` | 排行榜页 `34/34`，用户报告浅深色长按面板通过；真实举报记录按合同保留 |

## Standards 审查

- 依赖方向未回退：domain/product 未引入 Flutter/l10n；本轮未修改 `pushup_domain.dart` 或会员配置文件。
- 用户文案进入中英文 ARB，生成文件同步；正式 UI 使用现有主题和 Flutter 原生组件，无新增 UI 依赖。
- `AccountController` 本地恢复和 `LeaderboardController` 屏蔽名单均保留 generation/session 竞态守卫。
- Worker 新列表路由复用公开身份过滤，匿名或已退出用户不泄露账号昵称/网络头像；查询使用绑定参数。
- 未发现凭据、JWT、私钥或生产配置值进入分支差异；用户未跟踪交接文档未修改、未暂存。
- 结论：0 个硬性规范违规，0 个需要阻止合并的判断项。

## Spec 审查

- 所有明确需求均已实现，没有发现缺失项或与需求相反的行为。
- 首页顶部位置已按用户纠正恢复为左侧个人头像、右侧今日记录，不沿用最初误解。
- 屏蔽后保留全局名次并允许出现名次跳号，符合隐私过滤而不篡改全局排行的产品合同。
- 启动页只等待本地账号缓存和引导标记，远端 `/me`、会员和排行榜继续后台刷新，避免人为延长首屏。
- 长按入口只对其他用户生效，保留无障碍语义；低频审核操作不再破坏卡片末端对齐。
- 结论：0 个缺失需求，0 个无依据的范围扩张，0 个实现方向错误。

## 验证证据

- 本会话：`flutter analyze` 0 issue；Flutter `390/390`；Worker `126/126`；`git diff --check` 通过。
- 回放基线仍由全量套件守护为 `5/5/3`。
- production Debug APK 使用本机受保护配置覆盖安装到模拟器，保留登录数据；用户确认长按面板最终验收通过。
- 线上屏蔽名单 Worker 路由鉴权冒烟通过；本轮档案审核没有新增 Worker/D1/平台写入。

## 发布与后续边界

1. 已发布的 `0.3.6 (7)` AAB 源提交是 `a88ee5a`，不包含本报告后半段的新功能；当前 Debug 验收不能记为 Play 安装版验收。
2. 当前 `pubspec` 仍为 `0.3.6+7`，只用于本地开发覆盖安装。下一次 Play 候选必须提高版本号/代码，并在 main 审核合并后重新构建。
3. Alpha/Production 前仍需完成 Google Play UGC、Data safety 与内容分级声明；任何上传或轨道推进需要单独授权。
4. legacy Cloudflare Token 撤销/轮换仍是独立安全任务；已有专用部署 Token 可用不代表旧 Token 已撤销。
5. 零次训练云同步失败队列是本分支之外的既有债务，已保留在当前待办，不应通过放宽 Worker 校验解决。
